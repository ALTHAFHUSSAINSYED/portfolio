# MongoDB Commands to Check Portfolio Collections

# List all collections in your database
show collections

# Check for projects data
db.projects.find().pretty()

# Check for blogs data
db.blogs.find().pretty()

# Check for skills data
db.skills.find().pretty()

# Check for portfolio data
db.portfolio.find().pretty()

# Check for chat logs
db.chat_logs.find().pretty()

# Check for agent logs
db.agent_logs.find().pretty()

# If you need to create some test data:
db.projects.insertOne({
  "id": "test-project",
  "title": "Test Project",
  "description": "This is a test project to verify database connectivity",
  "created_at": new Date()
})

# After inserting, check again
db.projects.find().pretty()