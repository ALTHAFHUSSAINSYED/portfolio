# Updated for EC2 deployment
# Trigger deployment with new Gemini API key
# Deploy with correct ChromaDB API key (All databases scope)
# Redeploy with new ChromaDB API key

## GitHub Actions Deployment
- The Deploy Backend to EC2 workflow runs automatically when files under `backend/` change on `main`.
- Confirm successful runs from the Actions tab before testing the API.
